import React from "react";

export default function UserInfo({ r }) {
  return (
    <>
      <ul>
        {/* {r?.participants.map(
          (rr) => (
            <li>{rr}</li>
          ),
          console.log("r:", r)
        )} */}
      </ul>
    </>
  );
}
